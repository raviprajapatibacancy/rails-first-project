<p style="color: green"><%= notice %></p>

<%= render @tag %>

<div>
  <%= link_to "Edit this tag", edit_tag_path(@tag) %> |
  <%= link_to "Back to tags", tags_path %>
  <br><br>
  <%= button_to "Destroy this tag", @tag, method: :delete, class: 'btn btn-danger' %>
</div>
